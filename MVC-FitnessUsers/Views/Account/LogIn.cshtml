<h2>Din profil</h2>

<h4>Indtast brugernavn</h4>
@using (Html.BeginForm("UserProfile", "Account"))
{
    <div class="form-group">
        @Html.TextBox("UserId", "", new { @class = "form-control" })
    </div>
        <div class="form-group">
            <input type="submit" value="Log in" class="btn btn-default" />
        </div>
}
